# Адрес реального бэкенда (направление C). Если переменная задана — клиент ходит в API.
# Если переменная НЕ задана (закомментирована) — фронтенд работает на встроенных моках.
# VITE_API_URL=http://localhost:8000
