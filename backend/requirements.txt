fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
neo4j==5.27.0
elasticsearch==8.15.1
httpx==0.28.1
numpy>=1.26,<3          # chunk_vectors: loads graph/embeddings/*.npy at runtime
python-dotenv==1.0.1
pytest==8.3.4
PyJWT==2.10.1
# --- C-platform (C13–C18) ---
python-jose[cryptography]==3.5.0   # JWT (RBAC/ABAC)
structlog==26.1.0                  # JSON-логи / наблюдаемость
openpyxl==3.1.5                    # xlsx-экспорт compare-таблиц
Markdown==3.10.2                   # md -> HTML для pdf-экспорта
weasyprint==69.0                   # pdf-экспорт (fallback: reportlab/HTML, см. exporters.py)
# --- Upload-pipeline (POST /api/upload): reuse ingest+extract in-process ---
python-multipart==0.0.20           # multipart/form-data (UploadFile)
pymupdf==1.28.0                    # PDF text extraction (pipeline.ingest.textextract)
python-docx==1.2.0                 # docx/docm extraction
python-pptx==1.0.2                 # pptx extraction
xlrd==2.0.2                        # legacy .xls extraction
lxml                               # docm raw-OOXML fallback
PyYAML                             # concepts registry (pipeline.extract.normalize)
jsonschema                         # structured-output validation (llm_gateway)
